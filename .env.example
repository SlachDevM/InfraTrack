# InfraTrack Production Configuration Template
# Copy this file to .env and fill with real values for deployment
# Never commit .env with real secrets

# ========== DATABASE ==========
POSTGRES_DB=infratrack
POSTGRES_USER=appuser
POSTGRES_PASSWORD=your_secure_password_here

# ========== BACKEND DATABASE CONNECTION ==========
SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/infratrack
SPRING_DATASOURCE_USERNAME=appuser
SPRING_DATASOURCE_PASSWORD=your_secure_password_here

# ========== BACKEND - AUTHENTICATION ==========
# JWT secret key (generate a strong random value in production)
JWT_SECRET=your_jwt_secret_key_here

# ========== BACKEND - FIREBASE ==========
# Path to Firebase service account JSON file (outside Git)
FIREBASE_SERVICE_ACCOUNT_PATH=/app/firebase-service-account.json

# ========== BACKEND - EMAIL (SMTP) ==========
SPRING_MAIL_HOST=smtp.provider.com
SPRING_MAIL_PORT=587
SPRING_MAIL_USERNAME=your_email@provider.com
SPRING_MAIL_PASSWORD=your_email_password_here
SPRING_MAIL_PROPERTIES_MAIL_SMTP_AUTH=true
SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_ENABLE=true
SPRING_MAIL_FROM=noreply@yourdomain.com

# ========== BACKEND - FRONTEND COMMUNICATION ==========
# CORS allowed origin for frontend
FRONTEND_ORIGIN=https://yourdomain.com

# ========== BACKEND - ACCOUNT ACTIVATION ==========
# Activation link base URL (MUST BE CUSTOMIZED PER APPLICATION)
# - For web applications: Use HTTPS URL (e.g., https://yourdomain.com/activate)
# - For mobile applications: Use deep link (e.g., yourapp://activate-account)
# - The secure activation token will be appended as ?token=<secure-token>
# Default placeholder: app://activate-account (for development purposes)
ACTIVATION_LINK_BASE_URL=https://yourdomain.com/activate
# Token expiration in hours
ACTIVATION_TOKEN_EXPIRATION_HOURS=24

# ========== BACKEND - HIBERNATE ==========
# For production: validate (do not auto-create/update)
# Requires proper database migration strategy
SPRING_JPA_HIBERNATE_DDL_AUTO=validate

# ========== BACKEND - OPERATIONAL DOCUMENT STORAGE ==========
# Local filesystem path inside the backend container (UC-012).
# Docker Compose prod mounts operational-documents-data to this path by default.
# Override only when using a custom mount location.
OPERATIONAL_DOCUMENTS_STORAGE_PATH=/app/data/operational-documents

# ========== BACKEND - PROFILE ==========
# Local development: dev, Production: prod
SPRING_PROFILES_ACTIVE=prod

# ========== FRONTEND - API ENDPOINT ==========
# React Vite environment variable for backend API URL
VITE_API_BASE_URL=https://api.yourdomain.com
