# InfraTrack Backend - Development Profile
# Loaded when SPRING_PROFILES_ACTIVE=dev (the default local profile)

# SQL logging for local debugging only
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
